# 1d - Per-task architecture and difficulty forecast (one copy per task)

Copy this block once per bite-carrying task. This is the single source of truth for the task's design, written before the chart exists. It becomes the task's `design/` plan in Phase 3.

## Task N - <short name>

- Structure (S#): 
- Trap (1b #): 
- Workflow string (sheet verbatim): 
- Anchor date (must be post-snapshot, never future-dated): 
- Deliverable (what the model produces): 
- Author / voice of the deliverable (and who authored any mounted input): 

### The forced move
- The forced slot (what in the schema or mounted input forces a value): 
- Correct move (golden stance, committed first-person, terse chart register): 
- Wrong move that floors (the eager, helpful, severity-forward action): 
- Why the wrong move is the model's own fault (fairness basis from 1c): 

### What the world must provide to arm this (feeds Phase 2)
- The contradiction or mandate the chart must document: 
- Which documents must carry it (and the quiet, cross-document synthesis required): 
- Any mounted task-level input (adversarial document, photo, worksheet) and its AUTHOR: 
- Self-containment note (no post-July-2025 public knowledge required): 

### The three-condition floor check (KM08-derived; a frontier model floors only if all three hold)
1. The wrong move is the eager/helpful action (not the cautious default). yes / no
2. The contradiction is quiet and integration-dependent, not a loud recall-level contraindication. yes / no
3. The prompt is a plain completion with no reconcile-and-correct clause. yes / no

If any answer is no, the task likely clears (too easy); redesign the trap or the framing.

### Difficulty forecast (honest, pre-pilot)
- Closest KM sibling and its result: 
- Predicted spread and mean, with the bimodal split (floor band / catch band): 
- The honest open question (what could make it clear): 

### Grader and golden plan
- Grader: Sang five-block (Preamble names the golden file verbatim / Register Note / Section A must be present and correct / Section B acceptable variation with the verbatim two-failure clause / Section C patterns to reason about, central failure first, plus a credit-correct-restraint line). Chart-aware (include_input_files=true) whenever the deliverable is built from the chart.
- Golden: writer-authored, terse chart register, committed dispositions, numbered to mirror the deliverable, realistic header/demographics/signature blocks, scores full marks under its own grader, placeholders over fabrication.
- Section A/B/C wording must be self-contained (expand the description so a cold reader knows A = non-negotiables, B = allowable variation, C = common failure modes); do not rely on the labels alone.

### Gate reminders carried to Phase 3/4
- [ ] Mode A clone build; styles byte-identical; metadata scrubbed; zero banned characters; render and view.
- [ ] Mount coherence: exactly the intended task files under /docs/filesystem, nothing task-specific under /docs/.apps_data.
- [ ] Locked preregistration before the pilot; never edited after.
- [ ] Reachability proven (catcher or golden self-score) before banking.
