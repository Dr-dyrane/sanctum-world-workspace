# OV05 A0.5 fairness gate - 2026-06-15 (off-text med-rec finding via a home-med-bottle photo)
Result: PASS.

- Fair finding, reachable from the chart: the home-medication-bottle photo (a real chart source any reconciliation must use) shows an over-the-counter ibuprofen not on the electronic home-med list; the chart states NSAIDs are avoided given CKD, so it is a documented contradiction and an active nephrotoxin in her AKI on CKD. Catching it is reading an available source, not a guess or a judgment call.
- Off-text + un-primed: the finding lives only in the image plus one quiet nursing breadcrumb; the electronic home list is "verified," so the model trusts it and may never examine the bottles. No reflex pulls the model to OTC NSAID use.
- Fair construction (KM08-v7 / OV04-v3 pattern, NOT propagation): nothing pre-written is false. The electronic list is genuinely incomplete because the OTC was never disclosed; the model integrates a new real source rather than correcting a planted falsehood. Plain prompt, no reconcile-and-correct clause naming the trap. Gate 0 satisfied by construction, not disclosure.
- Anti-paralysis floor + chart-aware grader: refusing to finish is not credit; the grader scores the response text against the known finding and does not need to read the image.
- Distinct from siblings: OV01 continue inpatient enoxaparin (cold knowledge, text); OV02 line-site infection (off-text text synthesis); OV04 CPAP adherence (off-text image, device report); OV05 unlisted OTC NSAID (off-text image, med-bottle photo).
- Frozen-world-safe: task-layer only (started deliverable + transfer-day nursing note + image); reuses the world chart; rendered through the canonical Epic renderer (verify_no_synthetic + verify_no_km_identifiers + no banned chars).
- Self-contained / item-3 clean: no post-July-2025 knowledge; that NSAIDs are nephrotoxic in CKD/AKI is standard, timeless practice.
- Bench: cold design-bench = plausibly bimodal (OV04-v3 shape; floors reconcile from the list and miss the photo, catchers OCR the bottles and catch the ibuprofen). Pilot to confirm.
- MOUNT = the started reconciliation + the transfer-day nursing note + the home-medication-bottle image.
