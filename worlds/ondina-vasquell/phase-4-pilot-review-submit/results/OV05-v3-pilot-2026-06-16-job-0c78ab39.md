# OV05 v3 (embedded quiet K-load) PILOT = TRAP FAILED (partial, 2 scored) - job 0c78ab39 - 2026-06-16

VERDICT: the trap did NOT work. Only 2 of 10 runs scored; the other 8 are X (the recurring enable_anthropic_api preflight infra false-positive), so the user is rerunning for the full distribution. Both scored runs CAUGHT and removed the potassium salt substitute, which was supposed to be the quiet floor.

## What the two scored runs did
- Attempt 1 (run 22feecff) = 0.92: caught all three. Salt substitute STOP, ibuprofen STOP, lisinopril CONTINUE TO HOLD (rejected the draft's resume).
- Attempt 10 (run fec08694) = 0.62: caught the salt substitute (DISCONTINUE, correct hyperkalemia-in-CKD-with-ACEi reasoning) and the ibuprofen, but kept the draft's RESUME lisinopril (with restart parameters and a BMP recheck).

## Why it failed
The salt substitute was not quiet. Both runs decoded "salt substitute" to a potassium chloride load and fired on hyperkalemia in CKD stage 3b with an ACE inhibitor being restarted, the textbook association. A medication reconciliation primes scrutiny of every med, so a discrete med hazard is loud here, not quiet. This is the same structural reason the ibuprofen ceilinged in v1/v2. OV05 has now ceilinged three ways (ibuprofen x2, salt substitute). My "reads as a diet item" prediction was wrong.

## The 0.62 is not a floor
The grader caps on the salt substitute, which both runs caught, so neither is capped low. The 0.62 is a deduction for resuming lisinopril. Resume-with-monitoring (Cr 1.6 near baseline, K 4.1 falling) is a defensible clinical judgment, not a clean error, so it is fairness-marginal as a floor and the grader scored it mid, not floor. The distribution sits high (catcher).

## Per the prereg (CEILING branch)
The v3 prereg pre-registered this exact outcome: "Most runs also remove the salt substitute -> the potassium-and-CKD link is primed -> retire, or try a subtler potassium source, one re-roll." Honoring it. RECOMMENDATION: retire the embedded-med-hazard approach for OV05; the model is too strong on med hazards in this genre. Get floors from OV06 (conflicting-authority) and the other open lanes. The rerun will give the full distribution, but the salt-substitute catch is robust across both scored runs, so expect a catcher (roughly 0.6 to 0.95). No FA/GA: neither scored run floored.
