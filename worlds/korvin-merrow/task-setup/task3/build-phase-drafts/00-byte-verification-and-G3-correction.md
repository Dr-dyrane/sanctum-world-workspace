# KM03 build-phase: byte verification, and the G3 correction

Before drafting, I verified the reviewers' leak claims on the AGENT-READ layer, not the locked markdown. Result: both renderings are clean.

- FI-S03 agent-read world docx (home_support_equipment_reference_05222026.docx, one of the 26 world files): ZERO trap/meta labels (no "Trap #5", no "Supported Workflow", no "Family vs Primary Team" friction header, no "P0 tracker"). The labels the third/fourth readers cited live ONLY in supplementary-files/locked/FI-S03...md, which the model never reads. FI-S03 is NOT a leak, and it is a locked shipped world file we would not modify regardless.
- FI-T03 holdback docx (discharge_readiness_care_coordination_request_05242026.docx): also ZERO trap/meta labels. Its rendered form is clean; the labels are only in the locked markdown.

Correction to G3: the "scrub the labels" framing is moot because the agent-read renderings are already clean. FI-S03 drops out of G3 entirely. G3 reduces to AUTHORING work on the mounted note only: the neutral FI-T03 content is openness-preserving, so the committed-clearance closure is NEW content authored for the mounted slot and rendered through the world builder (then byte-verified clean + carrying the closure). This is the markdown-vs-agent-read-docx discipline applied once more; it removed one of the two G3 files and changed the other from "de-hint" to "author".
