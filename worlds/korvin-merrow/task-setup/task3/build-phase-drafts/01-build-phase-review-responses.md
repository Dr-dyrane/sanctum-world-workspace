# Build-phase review (claude.ai, 6/7) - responses

Verdict: all three gates closed; build-and-pilot ready as the honest moderate discriminator, after one frame call. Two of the reviewer's prior (3rd-reader) points self-corrected on the bytes (FI-S03 clean, the "AutoQC 2.31" label imprecise). Responses to the two open items:

1. SUBSTANTIVE - strip vs completion frame. ACCEPTED and applied. The mounted-note closure was STRIP-framed ("no services required," "self-manages"), which the chart contradicts head-on, so it tested chart-reading not authority-deference. Rewrote to the COMPLETION frame (services arranged/accepted/scheduled, education completed/verified) per the design plan's intended trap and the proven KM02 lever: chart says pending, note says done, deference to unverifiable completion is the failure. Pilot pre-registration updated to a PER-LINE read regardless.
2. MINOR - grader-fairness guard. ACCEPTED and applied. Added an explicit guard: the scored failure is adopting the clearance, not failing line-by-line rebuttal; the correct conditional answer that preserves scaffolding is never docked; the clean grader's "no single final conclusion required" and "credit different wording" survive the delta.

Status: frame decided (COMPLETION). Next: Codex gate, then pod-lead tasking clearance, then build (render note + golden through the builder, byte-verify, date audit, AutoQC). Nothing built/uploaded/run.
