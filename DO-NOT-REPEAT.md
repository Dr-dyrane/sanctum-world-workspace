# DO NOT REPEAT - the cold-start mistakes ledger

Read this FIRST, cold, before any task or world work. It is the concentrated list of every mistake Korvin Merrow actually made, each written as a rule so it cannot be re-made by someone starting fresh. If you do nothing else, do not re-do these. Each line: the mistake, where it bit, then FIX. Deeper detail is in the linked docs.

How to use it: before building anything, state three relevant lines from this ledger as your "no-repeat receipt" (AGENTS.md guardrail 8). If you cannot, you have not read in yet.

Last updated: 2026-06-14. Sources distilled here: `docs/task-difficulty-lessons.md`, `docs/task-structure-dossier.md`, `docs/grader-guidelines-lessons.md`, `docs/clinical-voice-lessons.md`, `docs/reviewer-response-protocol.md`, `docs/git-workflow.md`, `TASK-RUNBOOK.md`, `reference/source/Raising_Task_Difficulty_Worked_Example.pdf`, and the `KM-vs-QUILL-SANCTUM-MAPPING-REVIEW.md`. KM08 AO first review added the bright-line draft-fairness wording; the Raising Task Difficulty worked example added the answer-key / noise / off-text signal gate; King P's 6/12 note added the legitimate-failure-over-raw-score gate. None of these are new mistakes; they are the ones we already paid for.

---

## 1. Sequencing and scope (the root cause of most of the rest)

- Built the world/chart FIRST and chose task structures AFTER, so difficulty had only the completion wrapper left and 8 of 10 tasks became the same shape (Abi: "monotony"). FIX: pick 5+ distinct structures at brainstorm, THEN build substrate to arm each; cap completion/draft-and-finalize at 1-2 per world. (`docs/task-structure-dossier.md`)
- Brainstormed traps apart from the task surfaces that force them. FIX: before any new internal medicine Brainstorm, fill `reference/workflows/internal-medicine-world-planning-canvas.md` so each task has a structure, forced slot, trap, source route, fairness route, grader mode, and reachability plan before the patient documents are imagined.
- Designed files before the tasks needed them. FIX: design tasks first; each essential file exists because a task needs it for a correct answer (the Quill order).
- Left an answer-key synthesis in shared world files, so the model only had to transcribe the conclusion (Raising Task Difficulty worked example). FIX: world files provide raw material; completed synthesis belongs out of the shared world layer unless every task legitimately needs it. If one task needs it, scope it as a task-level file and explain why the user lacks it.
- Planned against the old 15-20 world-file range after the 06/10 update. FIX: new worlds need at least 30 world-level files; task-level files are separate and do not count toward that minimum.
- Tried to ship a too-easy task as "moderate" (KM03 v1/v2.1 ~93-94; KM05 early). FIX: no moderate tasks. Every task needs >=1 genuine sub-90 failure, or redesign and re-pilot. Do not relabel "too easy" as "moderate."
- Overanchored on the percentage instead of the trajectory content. FIX: a task banks on at least one legitimate model failure that materially lowers deliverable quality or creates patient-harm or malpractice risk, not on the raw score alone. A high score can still contain a bankable failure; a low score is useless if the only issue is cosmetic.

## 2. Fairness of construction (Abi retired three tasks for this; the costliest pattern)

- Planted a false claim in the model's OWN draft with no instruction to correct it, then floored the model for propagating it (KM05, KM06, KM07 v2, KM08 v4.1). FIX, canonical AO 6/11 rule: before turning in any finalize-the-draft task, either prompt the model to correct errors in the draft or use placeholders in the draft for the model to complete in the finalized version. Prefer placeholders when preserving difficulty. (`docs/task-difficulty-lessons.md` sections 5-6)
- Treated re-attribution as a universal draft fix. FIX: re-attribution works for external or weak-source documents, not for a same-author started draft the prompt asks the model to finalize. Same-author draft traps need true placeholders or an explicit correct-errors instruction.
- Added a reconcile-and-correct clause to a propagation task and killed the difficulty (KM06 v4 went to ~0.98 all-catch). FIX: reserve the reconcile clause for genuine judgment traps; never add it (or stance hints to the prompt) to chase a clean board.
- Re-attributed a claim but added an "unverified" caveat, telegraphing the catch. FIX: attribution only; no caveat, or you hand the model the answer.
- Treated an all-floor spread as automatically bankable. FIX: if the clinical stance is contestable, prove the catch is reachable with a catcher run or the golden scoring ~0.85-0.95 under its own grader. If the trajectory shows a legitimate external-query failure and the correct answer is structurally reachable, bank on the real failure rather than the mean.
- Answered a CDI or external query by declining on PROCEDURAL grounds ("a diagnosis cannot be added after discharge") instead of CLINICAL grounds, restated the absence of a diagnosis without explaining why, and built the decline on ancillary observations (nursing, OT) rather than the treating physician's own documented assessment (KM10 v1, reseeded by Abi 6/11). FIX: a CDI query is answered post-discharge by design, so the integrity-correct answer engages the indicators and explains WHY the record does not support the diagnosis, in the treating clinician's voice anchored on the treating note. Write the golden and FA as a reasoned clinical determination, not a bare "symptoms only, decline."
- Called a rebuilt task fair by reviewing the DESIGN PLAN'S description of the draft instead of the built artifact; the plan said "placeholder" while the built draft listed the trap item under "Current medications" beside an explicit held list (KM07 v3, caught by Abi 6/11; the v3 "fix" re-planted the v2 lie quietly). FIX: fairness review runs against the built draft's extracted bytes and QUOTES its verbatim lines about the scored item; runbook gate A0.5; reviewing a plan is not reviewing a task.
- De-telegraphed a draft by making the trap item "look as routine as the others," which is the same act as asserting it (KM07 v3 current-meds listing; KM08 v4.1 pre-written uptitration order surviving the de-telegraph pass). FIX: a true placeholder asserts NOTHING about the trap item anywhere in the draft; looking routine = asserting; a status disclaimer does not un-assert membership; a pre-written plan order is an assertion.
- Cleared the rest of the queue by memory after one task failed a fairness class, instead of re-running the test on every sibling construction (KM07 post-mortem cleared KM10 by genre but never re-checked KM08, whose built draft pre-writes the scored gabapentin order with a finalize-only prompt). FIX: when a construction class fails once, run the gate on the built bytes of every staged or in-review sibling the same day; findings to taskN/qa/.
- Treated a duplicate mounted-file leak as a filename problem when the real problem was an extra Studio volume (KM07 v4 job 6b687360: main `/docs/filesystem` draft plus unexpected `/docs/.apps_data/calendar` draft disagreed on alendronate; KM10 v2 job 138e90a2 confirmed the same calendar-volume defect with the CDI query memo). FIX: fairness review includes the agent-visible filesystem after upload: `find /docs -type f`, `/docs/filesystem`, `.apps_data`, filenames, duplicate preloaded volumes, and any file whose name says clean/fixed/revised/reviewer. Exactly one task file may be visible, under the intended filesystem volume. If `.apps_data/calendar` contains a task file, delete that Studio volume; do not rename around it.

## 3. Difficulty (these make a task too easy and get it bounced)

- Put the plant on the WARM/headline axis the model is already hunting (KM06 orthostatic 93, echo 97; KM03 v1 restart 94; KM07 v1 from-scratch 93.8). FIX: cold/background axis only; the deliverable's own subject is warm by default.
- Used a from-scratch synthesis or an unambiguous determination with no forced move (KM07 v1; KM08 v3 inpatient-vs-obs 96 because the case was not borderline). FIX: a task floors only with a forced slot (a required disposition the schema forces); design determinations genuinely borderline or do not use the structure.
- Planted on something the chart is merely SILENT about, not something it CONTRADICTS (KM05 v4-pilot1 floored every run but unfairly). FIX: floor only on a claim the record contradicts or explicitly mandates.
- Used a +N-day interval claim the chart cannot reach (KM05 v2). FIX: a post-chart claim is propagated or declined for free; put the forced judgment inside the same encounter the chart governs.
- Gave the model one clean prose path to every non-negotiable. FIX: force reconciliation across multiple raw sources, add realistic task-level noise or a required template, and consider an off-text critical signal such as a photo or handwritten list when clinically realistic. If the central miss would be indefensible in care, the grader must make it a hard error.
- Counted cosmetic or nonclinical misses as difficulty. FIX: missing a logo, minor formatting, or polish does not count as a legitimate failure. The failure has to be clinical, safety-relevant, coding or documentation integrity relevant, or otherwise material to the deliverable.

## 4. Temporal anchors (the most recurrent mechanical miss)

- Future-dated the golden (KM07 v1 golden dated 06/23). BANNED, non-negotiable. FIX: pin in-world today from the live prompt; nothing future-dated, ever.
- Anchored a task pre-snapshot / as a late-entry note (KM08 v3 HD1 05/18, v4 HD5 05/22). The pipeline does NOT catch this; only human review does. FIX: every encounter AND deliverable strictly after the world snapshot.
- Let a framing change leave surviving artifacts unverified (KM02 shipped a 05/23 golden into a 05/24 escalation). FIX: on ANY framing change (clean->escalation, reseed, date/mechanism shift) re-audit EVERY surviving artifact's dates, voice, status fields, and the grader's chart-access setting. "It passed under the old framing" is not verification. (AGENTS.md guardrail 5)

## 5. Build / DOCX (mechanical, each cost a round)

- Verified world facts against the markdown / .meta convenience copies instead of the agent-read docx (recurred in KM01 and KM02). FIX: verify on the agent-read docx at `file-review/upload/filesystem/` with python-docx INCLUDING table cells - the layer the model actually reads.
- Built a task docx from a blank `Document()` or from `generate_reference_files.py`, causing fingerprint drift and a re-injected "Synthetic training document" footer (KM01; KM08 v1). FIX: Mode A clone a proven approved artifact, styles.xml byte-identical, scrub core metadata, fingerprint-diff to zero.
- Scanned only document.xml for banned tokens and missed the footer string in footer1.xml (KM02, KM08). FIX: scan ALL xml parts; RENDER and visually view every docx before staging.
- Left an em/en-dash, arrow, bracket, or asterisk in a task docx. FIX: zero banned characters in authored content; swap chrome em-dashes to hyphens.
- Trusted a truncated sandbox read or an interrupted write (KM02 builder; the 6/10 mass truncation). FIX: integrity-gate after every save (EOCD + styles.xml + opens) and confirm the last section is present.

## 6. Grader

- Ran a golden-only grader on a SYNTHESIS task, so it flagged true chart detail as fabrication and turned the spread into noise (KM07 v3: identical behavior scored 0.30 vs 0.70). FIX: the grader must be able to read the provided chart whenever the deliverable is built from the chart; golden-only only for a planted-artifact catch. (`docs/grader-guidelines-lessons.md` Lesson 3)
- Wrote the grader in the instruction-doc A/B/C format and tripped the AutoQC gate (KM01). FIX: the Sang five-block - Preamble (names the golden file verbatim) / Register Note / Section A / Section B (with the verbatim two-failure-mode clause) / Section C (verbatim "patterns to reason about" opener, central failure first, plus a credit-correct-restraint pattern). ~1 page, no numeric weights / score caps / pass bands.
- Let the grader penalize something the golden itself does, or dock a correct withhold. FIX: never penalize what the golden does; explicitly credit keeping the trap item open/unverified.
- Let prompt/golden/grader quality vary while testing the trap (Sang: "the whole frame matters"). FIX: build all four surfaces to standard before reading a pilot, or the spread does not reflect the trap.

## 7. FA / GA / PL / QA annotations

- Wrote FA/GA in the both-sides format and named "Section A/B/C" (pre-Abi 6/9). FIX: failure-only (what the model did poorly / what the grader did poorly); spell out the section's content, never its name; the 2nd-LOWEST DISTINCT scoring run (King P 6/14 / #20, supersedes the earlier "single lowest run"); two paragraphs each; no grader-rating line in the prose (it is a Studio field); each part under ~1000 chars.
- Wrote FA around the lowest score when the score did not represent the real failure. FIX: read the output and grading transcript first. FA needs a defensible clinical or material deliverable failure; if the lowest run is cosmetic but another trajectory carries the real miss, escalate and document the selection rationale before writing.
- Used the bare words "tech issue" / "known issue" / "N/A" on a QA flag - failed as dismissive deflection (KM07 6/11). FIX: substantive, fact-referenced rebuttal in both boxes, citing that the grader actually ran (job id + per-run scores).
- Justified a QA thumbs-down with "the reviewer said it's okay." FIX: annotations are client-visible and must stand on their own reasoning.
- Drafted PL without the actual A/B pair, or skipped the cadence. FIX: three PLs per task from KM03 on, each on a different trajectory, against the golden; run PL AutoQC after each.

## 8. Process / hygiene / git

- Re-piloted without a locked preregistration (KM07 v3), forcing a post-hoc reconciliation. FIX: lock the prereg (forecast + read rules) BEFORE every pilot; never edit it after.
- Put .py files or loose planning .md at task-folder roots. FIX: scripts in `tools/`; planning in `design/`/`runs/`/`fa-ga/`; uploadable sets only in `platform/taskN/current/`.
- Told the user git was impossible when `rm`/commit failed with "Operation not permitted." FIX: the sandbox is create/overwrite-only until you call `mcp__cowork__allow_cowork_file_delete`; once approved, full git works (`docs/git-workflow.md`).
- Ran `git add -A` and swept unrelated working-tree changes (an in-progress dashboard rewrite) into an unrelated commit (KM10 v2, 6/11). FIX: stage the specific paths you actually changed (`git add path1 path2 ...`), never `-A`, so someone else's WIP is not bundled under the wrong commit message. Run `git status` first and look at what is modified before staging.
- Wrote a rule in instance-shape right after one error, so its siblings re-bit (the markdown/docx, framing-date, and template misses each fixed only their own case). FIX: write every rule at the CLASS it belongs to, not the single instance.

---

The meta-lesson behind all of these: none was a one-off slip. Each was a class of error we now have a gate for. The point of this file is that the gate travels with the repo, so a cold start inherits the scar tissue without paying for it again.

## Canonical docx template rule (added 2026-06-12, paid for twice)
- Built a project docx OUTSIDE the one canonical Epic renderer: the world files first pass used a gray/black approximation, and the OV01 golden v1 was hand-built as a plain Table-Grid note. Both diverged from the KM Epic template and had to be rebuilt. FIX, now a CANONICAL RULE: every docx in this project - world files, supplementary, task-level files (E1-T*), and task-setup goldens - is rendered through the single Epic template via build_world_files.build_one / epic.py (KM design system: Arial, navy/blue/light-blue, masthead, blue bar, patient storyboard, PATIENT/ENCOUNTER block, blue table headers, clean Ondina running header and footer). KM applies this same chrome to its world AND task-level files, with the issuer named in the title and filing line for external surfaces. NEVER hand-build a docx with a bare Document(); if a new docx type is needed, add a spec and route it through build_one. Verify fills and colors are a subset of the world-file vocabulary before calling any docx done.

## 5b. Build / DOCX, continued (Ondina run, 2026-06-12)
- Cloned a Mode A base and cleared only the body, leaving the PRIOR-WORLD patient name and MRN in the running header and footer (Ondina inherited Korvin Merrow in header1.xml from the progress base). FIX: clear and rebuild every header and footer part on every clone, and run verify_no_km_identifiers across ALL xml parts (not just the synthetic-token scan). clear_body does not touch headers or footers.
- Matched the KM design from memory and shipped a gray/black approximation instead of the navy/blue/light-blue system. FIX: match KM by EXTRACTING the recipe from the bytes (generate_reference_files.py constants and the shipped file fingerprints), never by approximating from memory. Verify the new file's fills and colors are a subset of the world-file vocabulary before calling it done.
- Placed task-level files only in task-files/ and forgot to copy each into its platform/taskN/current/. Task files upload WITH their task, never in the world-file set. FIX: a copy of each E1-T file lands in its task folder (build_task_packages.py copy step); task8 has none by design.
- Committed a duplicate-suffixed artifact (payer_concurrent_review_request_05252026 2.docx) while the canonical-named file was missing. FIX: filenames must match the spec file plan exactly; no " N.docx" duplicate-suffixed files; the gate fails on any name not in the plan.

## 9. Single source of truth and anti-hallucination (Ondina run)
- The same clinical value lived in several files (substrate pack, clinical_data.py, goldens, task files) and drifted mid-run (toe pressure 38 then 55). A contradiction across files reads as fact to the model and to a reviewer. FIX: the ratified substrate pack is the single source of truth; when a value changes, change it once and re-run `tools/verify/verify_ondina.py`, which cross-checks the key anchors across every docx and fails on any contradictory variant. Prefer referencing shared constants over re-hardcoding a value in a new builder.
- Transcripts carried benchmark register ("the model"). FIX: every reviewer-facing transcript is sanitized - no benchmark or eval register (the model, grader, golden, AutoQC, RLS), no internal tooling (Mode A, fingerprint, scrub, file paths), no prior-world identifiers; preserve only the real share URL and run id.
- Speed without capture loses the lessons. FIX: when gates are collapsed to run fast, write the run retrospective (worlds/<world>/OV-RUN-RETROSPECTIVE.md shape) and run the one-command gate before any stage; do not rely on memory of what was checked.

## 10. Spec drift from the approved brainstorm (Ondina Task 5, Spec AutoQC 2026-06-13)
- The brainstorm had Task 5 requested by the infectious disease attending (physician); the spec re-authored it as a "pharmacist-to-prescriber response note, requested by the inpatient pharmacist," which failed the All-Tasks-Physician-Produced gate (a pharmacist deliverable is a non-physician role, the MTM/CMR fail example). Root cause: optimized for role realism at spec-authoring and lost a hard platform convention; no brainstorm-vs-spec role diff was run. FIX: before the spec is final, (a) diff every task's requester, Format, and Register against the approved brainstorm and reconcile any drift, and (b) run reference/checklists/spec-autoqc-preflight.md criterion-by-criterion locally, including "every task deliverable is physician-produced" - the requester may be any role, but the authored deliverable's voice/format must be a physician's. Local hygiene gates (verify_ondina, A0.5) do NOT cover task-design criteria; run the spec preflight separately.

## 11. Spec file-table format (Ondina spec review, Larry 2026-06-13, cost a spec-review round)
- The world-spec file table shipped with 8 columns (# | ID | Filename.type | Source | Tool | Reference File Origin | Description | Pearls), with a redundant Source/Tool pair, and all-portrait so the rightmost column clipped off the page. Sent back. FIX, canonical: match the approved KM spec table EXACTLY - 7 columns: # | ID | Filename.type | Date | Reference File Origin | Description | Pearls, Traps, and Friction. No Source column, no Tool column; fold the source role into the origin cell; carry a Date / hospital-day anchor column.
- Reference File Origin uses the four-option convention WITH the filename in parentheses so engineering knows what to do per file: Public Domain (existing form, e.g. FMLA/OSHA) / Custom Made (template.docx) [writer drafts a template, engineering converts] / Databank template (filename.docx) [Mercor internal library] / Writer produced (filename.docx) [finished file, engineering does NOT touch]. Rule from Larry: if you already engineered the file, write Writer produced (filename). Our Mode-A finished docx and images are all Writer produced, so engineering does not regenerate them and trap fidelity is preserved.
- Wide tables must sit in a LANDSCAPE section (11x8.5) with fixed column widths summing to the text area, or Word clips the right column. KM uses a portrait -> landscape -> portrait section layout for exactly this. The spec builder now switches orientation around section 3 (build-docx-ondina-worldspec.py).

## 12. The spec doc is a SEPARATE source the gate does not cover (Ondina Spec AutoQC, 2026-06-13)
- The F2 attending rename (Marisol Everet -> Lillian Everet) and the baseline ambulation correction ("ambulates with a cane" -> "without a device but with reduced endurance") were fixed at the canonical source (clinical_data.py), the built world files, and the goldens, but NOT in the spec markdown, which is a separate hand-maintained source. Spec AutoQC flagged the cross-section contradiction (roster vs every reference file; baseline vs the admission H&P). Root cause: verify_ondina scans world/task/golden docx only; the spec doc and the transcripts sit outside it. FIX: when any ratified value changes, grep EVERY source for the OLD value - the spec md/docx, the transcripts, and planning docs - not just clinical_data and the builders. Better: bring the spec's roster and baseline anchors under the gate (cross-check against clinical_data) so spec drift fails locally before AutoQC.

## 13. The workflow menu and pod guidance are LIVE and volatile (Ondina, Ed/Abi 2026-06-13)
- Workflows get retired from the platform menu mid-flight once they hit delivery target, and a world built against a retired workflow cannot create that task. Ondina had four retired AFTER the spec passed QC (T1 discharge med rec, T3 CDI query, T8 specialist referral letter, T9 patient safety event). FIX: keep ONE canonical workflow map (build_task_packages.py WORKFLOW) as the single source; verify every task's workflow against the LIVE Task Selection Categories sheet right before Step 10 (a snapshot drifts - ours was four days stale and priorities had already moved); name the workflow in every RUN-INSTRUCTIONS, KM-style ("## Workflow type:" header + "Workflow type =" as upload step 1).
- Workflow is SELECTED at Step 10 and spec-to-task divergence is legitimate (KM's shipped tasks differed substantially from its spec). So remap retired workflows AT tasking; do NOT re-upload an approved or in-review spec for a change that can wait for Step 10. Keep the retired-workflow designs (pod rule: do not delete; they may reopen).
- Layer discipline: a deferred change belongs to the layer that LOCKS it. World content locks at world generation (template/world-file layer - change before Stage 7); workflow, prompt, and golden are task-layer (Step 10). Park each deferred change at its correct layer, never lumped together.

## 14. Three different date rules - do not conflate (Ondina, Larry + Abi 2026-06-13)
- (1) Anchored-after-world-close: every task encounter and deliverable dated strictly after the world snapshot (internal ordering). (2) No-future-dated: nothing dated after the real today - a HARD rule the live QC blocks. (3) Knowledge-cutoff: the ASK and DELIVERABLE must not require public clinical knowledge published after July 2025. These are independent; passing one is not passing another.
- UPDATE 2026-06-13 (Abi, Vagus): the "shift world-file dates to PRE-July-2025" guidance was HYPOGLOSSUS-ONLY and is RESCINDED for Vagus. Do NOT shift OV world dates; 2026 date-stamps are fine as long as nothing is future-dated. OV never shifted - no action. (If a pod ever did shift, that is fine, proceed.)
- The July-2025 cutoff is about the model's CLINICAL KNOWLEDGE (guidelines, approvals), NOT the calendar. A 2026 encounter date is fine. The operative rule (new 6/13): the deliverable must not require knowing a post-July-2025 guideline or FDA approval (e.g., the 2026 AHA stroke guidelines, a drug approved 01/12/2026). Phrase prompts deliberately. For OV, spot-check OV07 (HEDIS measure-year spec) and OV03 (coding-guideline year) before pilot; general abstraction/coding logic is fine, a named post-2025 spec is not. Approved-workflows + this guidance mirrored at reference/approved-workflows-and-guidance-2026-06-13.md.
- PROMPT FIREWALL (canonical 2026-06-14): every task prompt that could drift into guideline, drug-approval, coding, payer-policy, or quality-measure knowledge says, in natural physician voice, that the answer should use only the attached chart and any attached policy/reference files. Do NOT write "use the latest guidelines," "use current standards," "apply 2026 guidance," "use the most recent FDA approvals," or "use current HEDIS rules" unless the exact source is attached and it is realistic for that workflow. Canonical clause for QC: "Use only the attached chart and any attached policy or reference files. Do not assume outside guideline updates, drug approvals, coding changes, or measure specifications that are not in the provided materials. If the record or attached reference does not support a finding, date, value, criterion, or recommendation, say so and leave it open rather than filling it in from general knowledge."
- Multilingual tasking is paused: all world and task files in English. A patient language preference (for example Spanish-preferred) is a profile attribute rendered in English with interpreter notes, not foreign-language file content.

## 15. Settled this run - do not re-raise these as open (2026-06-14)
- METADATA WIPE IS LOCKED AND AUTOMATED. `scrub_all_metadata` + `make_deterministic` run on every build (build_world_files.py), and `verify_ondina` fails any docx whose core metadata is not scrubbed. Verified: 0 of 29 world docx carry creator/lastModifiedBy. The pod rule "wipe metadata when changing a world file" is already satisfied by the build; do not treat it as an open task. On any Stage-9 edit, rebuild through the pipeline (which re-scrubs) or run scrub before re-upload.
- DATE POSTURE IS SETTLED. OV has no future-dated content (passed live QC). The pod rule "if you change a world file, also shift dates to before July 2025" is a CONDITIONAL trigger handled in STAGE-9-FILE-REVIEW-PLAN.md: if any world file is edited at Stage 9, apply the uniform pre-July-2025 shift across all world files in that pass; otherwise no action. Not an open item until Stage 9 editing occurs.
- RLS = RL Studio = Mercor Studio. We HAVE been in Studio (brainstorm, world spec, pipeline run). "Already created your task in RLS" means TASKING (Step 10), which we have NOT begun, so the workflow restriction DOES apply to OV (the four remaps stand). Do not confuse being in Studio for spec/pipeline with having created tasks.
- WRITER'S CHECKLIST is already embodied across our workflow (the task self-QC running AutoQC Section 4/5/6, the run-instructions, verify_ondina, the guardrails). Treat it as satisfied by our existing process, not a separate new gate.

## 16. The OV01 "world leak" was a TWO-TASK-FILE mount, not a world-file leak (Studio, 2026-06-14)
- Symptom: OV01 re-pilot (job 9765ba91) mounted TWO order sets - `/docs/filesystem/preliminary_discharge_order_set_05212026.docx` (old, telegraphing) AND `/docs/.apps_data/calendar/discharge_medication_orders_05212026.docx` (new, de-telegraphed). I misdiagnosed this as a world-file leak and sent the writer hunting the World Files for a `preliminary` file that was NEVER there (World Files = 34 clean docs, verified, no order set).
- Actual root cause, both on the TASK layer: (a) the de-telegraph rename ADDED the new task file without REMOVING the old one, so the task carried two order sets; (b) the new file was mis-tagged to the "Calendar" app, which mounts a task file under `/docs/.apps_data/calendar/` instead of `/docs/filesystem/`. Task AutoQC's duplicate check compares FILENAMES across task vs world, so the rename let two same-purpose files coexist undetected.
- FIX (task-file hygiene, every task): a task's reference file is a PLAIN Filesystem file unless the task genuinely needs an app surface; never leave it tagged Calendar/Email/etc. by accident. When you rename or replace a task file, DELETE ALL files in the Task Files card and re-add only the current one (the writer's fix). After any re-pilot, read the first trajectory's `find /docs` tree and confirm: one order set, in `/docs/filesystem/`, nothing under `/docs/.apps_data/`, no stale/old filename. A rename can DODGE the duplicate-name AutoQC check, so the mount tree is the real gate, not AutoQC.
- Diagnosis discipline (the hallucination): when a file appears in the agent mount, prove WHICH layer it came from (world revision vs task card) by reading the actual file lists before prescribing a fix. The World Files screen and our local `file-review/revision/filesystem` (34 files) both showed no order set; that alone disproved the world-leak theory. Do not send the writer to delete from a layer you have not confirmed contains the file.
- Stale local artifact compounded it: the abandoned Stage-9 `file-review/pipeline-output/` (never uploaded) still held old `preliminary` copies and kept reanchoring the wrong theory. Deleted 2026-06-14 (tombstone: `file-review/pipeline-output-REMOVED.md`). Keep discarded pipeline runs OUT of the live tree.

## 20. FA/GA subject = 2nd-LOWEST % score, not the lowest (King P office hours, 2026-06-14)
- King P, 2026-06-14: "For FA/GA, please use the 2nd lowest % score moving forward (vs. the lowest score)." This SUPERSEDES every prior "single lowest genuine-failure run" instruction (DO-NOT-REPEAT, abi-review-protocol, floor canon, prereg read-rules, KM tasking playbook, FA_GA template). Rationale (implied + matches our evidence): the very lowest run is often the noisy outlier (e.g., OV02's 0.62 was file-output noise; degenerate/inline/errored runs cluster at the bottom), so the 2nd-lowest is the more representative genuine failure.
- RULE: FA/GA is derived from the 2nd-lowest-scoring trajectory. It must still be a LEGITIMATE material failure (the legitimate-failure gate still applies); if the 2nd-lowest is somehow cosmetic/noise, record the rationale and pick the nearest legitimate failure with Alexander. GA still analyzes the grader's scoring of THAT (2nd-lowest) run. Format unchanged (failure-only, two paragraphs, no section names, no dashes, physician voice).
- APPLIES MOVING FORWARD: do not retroactively rewrite delivered KM FA/GAs. OV01 is banked but NOT submitted, so switch its FA/GA subject from the 0.40 run (attempt 3) to the 2nd-lowest, the 0.50 run (attempt 7 / job 741ba52f); needs that run's output + grading transcript to re-derive. All OV preregs/read-rules now say "2nd-lowest" not "lowest."

## 21. World files are FROZEN once tasking begins (King P pinned, 2026-06-14 9:38)
- "Once your world-level files are finalized, they should not change. If they get flagged with major concerns during tasking, we may pause your tasking until they're fixed, because world-level changes force every downstream step to be redone." So a world-file edit mid-tasking risks PAUSING the whole world and redoing trajectories/AutoQC/pipeline.
- RULE for OV: the world (Healthcare_297_Vasquell, 34 files) is finalized and frozen. Every remaining trap must ride entirely on the TASK layer (task file + golden + grader), never a world-file edit. OV01's enoxaparin-in-MAR world change is already done/uploaded/banked - no further world edits. Verified 2026-06-14 that OV03-OV10 mechanisms are all task-layer (OV07 undated-A1c rides on an existing world fact; OV03/04/05/06/08/09/10 are worksheet/denial/criteria/intake/draft task files). If any future mechanism seems to need a world-file change, redesign it onto the task layer or escalate - do NOT silently edit a frozen world file.
- Upload discipline (reinforces #16): when Studio asks for WORLD files, upload only world files; task files go up only during tasking, in their task, after vetting + reviewer sign-off. Keep world vs task in separate folders from the moment engineering hands them back.
- Date cutoff reinforcement: real-time web search is OFF on the platform; the model knows nothing published after July 2025. Item-3 (ask/deliverable must not require post-cutoff public knowledge) stands; workaround = upload the post-2025 guideline yourself as a file ONLY when that is a realistic workflow. OV is clean (all 10 swept).

## 18. Conversational prompt vs save-to-/tmp/outputs harness = file-output noise (Ondina, 2026-06-14)
- A conversational task prompt ("give me your attestation", "tell me line by line") lets runs answer inline, but the platform harness says "Output files MUST be saved in /tmp/outputs/", so the model-grader docks inline answers regardless of clinical quality. On OV02 v2 this FAKED the only sub-70: the 0.62 run caught every clinical trap (graded "~0.9+ content") and was docked solely for not saving a file. It also nicked OV01's 0.40 run; it just did not dominate there because OV01 had a real clinical floor underneath. The risk: file-save becomes the discriminator and fakes or masks the true clinical spread.
- FIX (ratified 2026-06-14): every task prompt names the deliverable as a saved document in the physician's own voice (e.g., "put your attestation in a document I can drop into the chart for sign-off"), so file-save stops being a discriminator and pilots read clean on clinical substance. Apply to OV02-OV10 prompts as each is built/recalibrated (OV01 is banked; do not retrofit/re-pilot it). Do not rely on the grader to ignore output location, and do not loosen a grader to erase this noise - fix it at the prompt.
- VOICE CORRECTION (OV07, 2026-06-14): do NOT write a literal /tmp/outputs filesystem path into a clinical prompt - a clinician would never dictate a file path, and it breaks the physician voice (and the All-Tasks-Physician-Produced register). The harness already routes any saved document to /tmp/outputs, so the prompt only needs to ask for a SAVED DOCUMENT in natural voice. Canonical form: "Put your attestation in a document I can sign, and give me a brief summary here." (The earlier explicit-path phrasing on OV02 was wrong on voice; OV02 is retired so moot.)

## 19. Cap pilots in a model-strong genre; retire/swap rather than grind (Ondina OV02, 2026-06-14)
- OV02 (coding attestation) ceilinged THREE times: v1 warm steers (0.92), v2 cold fabrication plant (0.87, the sub-70 was file noise), v3 a ratified procedure-depth judgment trap (0.94) - every planted upcode caught by every run. Coding attestation is a fully-reconciling "verify each proposed code against the chart" genre and this model is excellent at it, so loud, cold, AND judgment-depth fabrication traps all get caught. KM hit the identical wall (KM09 coding ~0.86 with one 0.55; KM10 CDI an all-floor killer). A pure ceiling is NOT shippable (Abi two-sided gate sends back all >=90).
- FIX: do not sink more than ~2 pilots into a genre the model is strong at. If two distinct, ratified mechanisms both ceiling, the lever is genre/trap-TYPE, not another plant: either a commitment/hedge trap (the one type that floored a coding task in KM - failure = leaving an unsupported option signable rather than removing it) or retire/swap the task. Not every task must floor; the suite needs a MIX, and the genuinely cold tasks (lookback/exclusion, forced-inventory, planted-closure) carry the floors. Preserve retired source (pop from generators, archive the package) for a possible later commitment-trap revival; do not grind while frustrated - that is how contrived/unfair traps get built.

## 17. Keep ONE running status/log, update it every step - do not rely on point-in-time docs (Ondina, 2026-06-14)
- For most of OV the only records were point-in-time files (calibration, prereg, results, FA/GA, AO review) created once and left; there was no running per-world status or activity log the way KM had (`task-setup/taskN/TASK*-STATE.md` + `docs/status-dashboard.md`). The writer flagged it: progress was scattered and existing files were not being updated as state advanced. FIX: maintain `worlds/<world>/OV-WORLD-STATUS.md` (or equivalent) as the SINGLE source of truth - a per-task lifecycle table plus an append-only dated activity log - and update it at every build/upload/pilot/bank/FA-GA/PL/review/submit step. Point-in-time docs are linked from it, not a substitute. State the current status line from it as part of the read-in receipt.

## 22. Any golden or grader change => re-run trajectories (Ondina, Kathy G review, 2026-06-15)
- Changing the golden OR the grader ALWAYS requires re-running the pilot (a fresh trajectory set) before bank/submit, even when the change looks behavior-neutral (a wording trim, a consistency fix, a length cut). The reference answer and the scorer both shape the score distribution; any edit can shift it, so re-measure. Do NOT claim "scoring logic unchanged, no re-pilot needed" (Claude did exactly this on OV02 after the Kathy G golden+grader fix - wrong). FIX: after any golden/grader edit, re-AutoQC then re-run trajectories; FA/GA and the bank decision read off the NEW run. Currently applies to OV02 (golden visual-claim fix + grader trim) and OV04 (grader length trim 806->530) - both need a confirming re-pilot before bank.
- GATE: tools/verify/verify_ondina.py now enforces grader length (<=540 words) and golden-vs-grader "do not credit visual detail" consistency; tools/verify/presubmit_task_gate.py runs the mechanizable reviewer-failure-patterns per task before submit. Run both before any upload.
