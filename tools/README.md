# Tools Directory

Do not store binaries, installers, package archives, credentials, browser profiles, or generated tool caches in this repository.

System tools for this workspace should be installed on the machine through approved package managers such as `winget` or `pip`.

Only lightweight documentation about tooling should live in the repository.

